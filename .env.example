# Copy this file to .env and fill in real values locally.
# Do not commit .env or real API keys.

FAL_KEY=

# Optional. UI defaults to http://localhost:3001 when this is not set.
BACKEND_URL=http://localhost:3001

# Optional. Leave empty when using the default Next.js rewrite.
NEXT_PUBLIC_ORCH_API_BASE=
NEXT_PUBLIC_ORCH_STREAM_BASE=
